{
  "version": "v1",
  "hooks": [
    {
      "name": "Typecheck on save",
      "trigger": "PostFileSave",
      "description": "Runs a TypeScript check whenever a .ts or .tsx file is saved, so type errors surface immediately instead of at build time.",
      "matcher": "\\.(ts|tsx)$",
      "action": {
        "type": "command",
        "command": "npx tsc --noEmit",
        "timeout": 120
      }
    }
  ]
}